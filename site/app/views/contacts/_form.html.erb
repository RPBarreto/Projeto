<%= form_for(@contact) do |f| %>
  <% if @contact.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@contact.errors.count, "error") %> prohibited this contact from being saved:</h2>

      <ul>
      <% @contact.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Nome %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :Email %><br>
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <%= f.label :Assunto %><br>
    <%= f.text_field :subject %>
  </div>
  <div class="field">
    <%= f.label :Mensagem %><br>
    <%= f.text_area :message %>
  </div>
  <div class="actions">
    <%= f.submit "Mandar mensagem"%>
  </div>
<% end %>
